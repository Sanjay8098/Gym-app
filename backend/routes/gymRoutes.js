const express = require('express');
const router = express.Router();
const GymController = require('../controllers/GymController');

// Gym routes (CRUD operations for members, classes, etc.)

module.exports = router;

